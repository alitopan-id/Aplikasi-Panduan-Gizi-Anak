<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.panduanmakansehatanak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menu2_1012Activity"></activity>
        <activity android:name=".Menu2_79Activity" />
        <activity android:name=".Menu2_46Activity" />
        <activity android:name=".Menu2_13Activity" />
        <activity android:name=".AngkaKecukupanGiziActivity" />
        <activity android:name=".CekStatusGiziActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>